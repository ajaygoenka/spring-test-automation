# Properties can be overwritten at runtime by passing in maven arguments using -D
# e.g. mvn clean install -Dcompare.api=http://c2.stg.experian.com/compare/api

# Based on Brand
external.api.base.creditmatcher=http://localhost
external.api.base.experian=http://localhost
external.api.base.prodmove=http://localhost

serviceregistry.table=local-service-registry
config.table=local-config

securetoken.url=http://localhost
securetoken.api.version=3.0.0

external.api.base=http://localhost:9000

billingrateplans.url=http://localhost
billingrateplans.api.version=3.0.0

ping.required=1


protocol=https://